# Server-side (Next.js API routes)
GEMINI_API_KEY=your_gemini_key_here
OPENAI_API_KEY=your_openai_key_here

# Frontend toggle (local-first MVP)
# - local: Upload page hits /api/local/extract (writes to data/events.json)
# - firebase: POST /api/upload → Admin SDK → Storage + OpenAI + Firestore `flyers` (no browser Storage CORS)
NEXT_PUBLIC_BACKEND_MODE=local

# Firebase (Web SDK config) — client Firestore reads (feed, etc.)
NEXT_PUBLIC_FIREBASE_API_KEY=your_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# Optional — only if auto-detection fails; exact name from Google Cloud → Cloud Storage → Buckets
# FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
NEXT_PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef123456

# Public site URL (no trailing slash). Required for branded password-setup links: admin "Create user" rewrites the
# Firebase reset URL to https://YOUR_DOMAIN/auth/action?... Add this domain under Firebase → Auth → Settings → Authorized domains.
# Local: http://localhost:3000
NEXT_PUBLIC_APP_URL=https://your-vercel-app.vercel.app

# In Firebase Console → Authentication → Sign-in method: enable Email/Password (and optionally disable Google if unused).
# Authorized domains: add localhost and your Vercel domain.

# Admin email — must exactly match the Firebase Auth email for the admin account (any domain: Gmail, school, etc.).
# Server-only: /admin APIs, create-user, delete flyers, upload gate; Gmail ingest should use this same mailbox when using OAuth below.
# New user profiles get canUpload=false until you toggle them on; the admin account always may upload.
# Create user (admin UI): new accounts must use @byu.edu and a BYU Net ID; /login accepts any email for existing Firebase users.
ADMIN_EMAIL=your.admin@gmail.com

# Optional: same value as ADMIN_EMAIL if the client needs to display or compare the admin email (e.g. UI hints).
# Sign-in accepts any valid email; domain is not restricted.
NEXT_PUBLIC_ADMIN_EMAIL=your.admin@gmail.com

# Firebase Admin (server uploads to Storage — required when NEXT_PUBLIC_BACKEND_MODE=firebase and bucket is set)
#
# Option A — file path (simplest locally; .secrets/ is gitignored)
FIREBASE_SERVICE_ACCOUNT_PATH=.secrets/firebase-adminsdk.json
#
# Option B — base64 the JSON file (works in a single .env line with Next.js; avoids dotenv mangling \\n)
#   base64 -i .secrets/firebase-adminsdk.json | tr -d '\n'   # paste result as one line:
# FIREBASE_SERVICE_ACCOUNT_JSON_BASE64=...
#
# Option C — raw JSON string (often breaks in Next: double-quoted .env lines rewrite \\n and corrupt the key)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# --- Optional: Slack flyer ingest (cron + optional instant Events API) ---
# Bot needs channels:history, files:read, and membership in each channel. Use channel IDs (C… / G…), not names.
#
# Instant ingest: POST /api/slack/events — set SLACK_SIGNING_SECRET, then in Slack app → Event Subscriptions enable
# and subscribe to message.channels (and message.groups for private channels). Request URL must be HTTPS public.
# Thread replies are ignored; top-level posts only (same rules as cron).
# SLACK_SIGNING_SECRET=from Slack app → Basic Information → Signing Secret
#
# Primary workspace:
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_CHANNEL_IDS=C0123456789,C0987654321
# Second workspace while waiting for admin approval on another (same app or a separate Slack app):
# SLACK_BOT_TOKEN_2=
# SLACK_CHANNEL_IDS_2=
# Or add more via JSON array (merged after primary + secondary):
# SLACK_INGEST_JSON=[{"token":"xoxb-...","channels":["C111","C222"]}]
# Default lookback is 2 days if unset. For frequent crons, prefer a short window (hours):
# SLACK_LOOKBACK_HOURS=2
# SLACK_LOOKBACK_DAYS=2
# Secure /api/cron/* ingest routes in production (Vercel Cron sends Authorization: Bearer … when set):
# CRON_SECRET=long-random-string
#
# Optional: set false on a public site so only cron/curl can trigger ingest (Upload page button calls the server action).
# SLACK_ALLOW_UI_SYNC=true

# --- Optional: Gmail inbox ingest (same Google account as ADMIN_EMAIL when that is Gmail) ---
# Create OAuth 2.0 Client ID (Desktop app or Web) in Google Cloud Console, enable Gmail API, then obtain a refresh token
# with scope https://www.googleapis.com/auth/gmail.readonly.
# GMAIL_CLIENT_ID=....apps.googleusercontent.com
# GMAIL_CLIENT_SECRET=GOCSPX-...
# GMAIL_REFRESH_TOKEN=1//0...
# Optional overrides for listing (default: in:inbox newer_than:2d, max 25):
# GMAIL_INGEST_QUERY=in:inbox newer_than:1d
# GMAIL_INGEST_MAX_RESULTS=30
